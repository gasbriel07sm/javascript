// Usaremos o formato camelCase

// não podemos declarar variáveis com dígitos no início
// não podemos declarar variáveis com palavras reservadas da linguagem
// não podemos declarar variáveis com espaços ou símbolos, exceto _ (underline) e $ (cifrão)

// podemos declarar variáveis em inline (mesma linha):
let nome, sobrenome;