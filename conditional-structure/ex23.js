const num = 12;

if (num === 0) {
  console.log('Zero');
} else if (num > 0) {
  console.log('Positive');
} else {
  console.log('Negative');
}

// console.log(`${
//   num === 0 ? 'Zero' : num > 0 ? 'Positive' : 'Negative'
// }`)